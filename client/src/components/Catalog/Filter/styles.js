import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
   marginTop: 40,
   marginBottom: 40
  },
  selectUno: {
      width: 300,
      display: 'flex',
      flexDirection: 'flex-start'
  },
  selects: {
    width: 150
  }

}));